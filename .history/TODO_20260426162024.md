# Fix Plan

## Issue 1 — HTTP 400 on `/by-public-id/{uuid}`

- [x] Analyze `resolvePublicId()` parallel request problem
- [x] Update `frontend/src/api/client.js` — add `silent` header support
- [x] Update `frontend/src/api/users.api.js` — sequential lookup + silent requests
- [x] Update `backend/GlobalExceptionHandler.java` — return 404 for "not found"

## Issue 2 — WebSocket connection failures

- [x] Update `frontend/src/hooks/useLiveUpdates.js` — fix broker URL to `/api/v1/ws`
- [x] Update `backend/WebSocketBrokerConfig.java` — add `ChannelInterceptor` for JWT auth
- [x] Build backend to verify compilation
- [x] Re-test frontend
