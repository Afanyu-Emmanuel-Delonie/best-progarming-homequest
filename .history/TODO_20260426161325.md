# Fix Plan

## Issue 1 — HTTP 400 on `/by-public-id/{uuid}`

- [x] Analyze `resolvePublicId()` parallel request problem
- [ ] Update `frontend/src/api/client.js` — add `silent` header support
- [ ] Update `frontend/src/api/users.api.js` — sequential lookup + silent requests
- [ ] Update `backend/GlobalExceptionHandler.java` — return 404 for "not found"

## Issue 2 — WebSocket connection failures

- [ ] Update `frontend/src/hooks/useLiveUpdates.js` — fix broker URL to `/api/v1/ws`
- [ ] Update `backend/WebSocketBrokerConfig.java` — add `ChannelInterceptor` for JWT auth
